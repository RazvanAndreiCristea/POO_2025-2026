#pragma once
#include "Sticla.h"

namespace VectorHelper
{
    void interschimbareSticle(Sticla& s1, Sticla& s2);
    void sortareSticle(Sticla*& sticle, const int& nrSticle);
    void afisareSticle(Sticla*& sticle, const int& nrSticle);
}